package com.saleh.wadhkur;
import android.Manifest;import android.app.*;import android.content.*;import android.content.pm.PackageManager;import android.os.Build;
public class ReminderReceiver extends BroadcastReceiver{private static final String CH="wadhkur_reminders";private static final int ID=881;
 public void onReceive(Context c,Intent i){if(Build.VERSION.SDK_INT>=33&&c.checkSelfPermission(Manifest.permission.POST_NOTIFICATIONS)!=PackageManager.PERMISSION_GRANTED)return;NotificationManager n=(NotificationManager)c.getSystemService(Context.NOTIFICATION_SERVICE);if(n==null)return;if(Build.VERSION.SDK_INT>=26){NotificationChannel ch=new NotificationChannel(CH,c.getString(R.string.channel_name),NotificationManager.IMPORTANCE_HIGH);ch.setDescription(c.getString(R.string.channel_description));ch.enableVibration(true);n.createNotificationChannel(ch);}DhikrData.Dhikr[] d=DhikrData.REMINDERS;if(d.length==0)return;String t=d[(int)(System.currentTimeMillis()%d.length)].text;Intent o=new Intent(c,MainActivity.class);o.setFlags(Intent.FLAG_ACTIVITY_NEW_TASK|Intent.FLAG_ACTIVITY_CLEAR_TOP);PendingIntent p=PendingIntent.getActivity(c,991,o,PendingIntent.FLAG_UPDATE_CURRENT|PendingIntent.FLAG_IMMUTABLE);Notification.Builder b=Build.VERSION.SDK_INT>=26?new Notification.Builder(c,CH):new Notification.Builder(c);b.setSmallIcon(android.R.drawable.ic_popup_reminder).setContentTitle("🌿 وذكر").setContentText(t).setStyle(new Notification.BigTextStyle().bigText(t)).setAutoCancel(true).setContentIntent(p).setPriority(Notification.PRIORITY_HIGH).setVibrate(new long[]{0,220,120,220});n.notify(ID,b.build());MainActivity.recordDhikr(c);}}
